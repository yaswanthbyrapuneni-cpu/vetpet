/** How long a call rings before the caller gives up and the callee's incoming-call popup
 * auto-dismisses. Shared by both sides so they can't disagree on when a call should time out. */
export const RING_TIMEOUT_MS = 45000
